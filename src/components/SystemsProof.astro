<section class="proof-section" id="proof">
  <div class="proof-section-inner">
    <div class="site-section-head" data-reveal>
      <h2 class="site-section-title">Systems running right now</h2>
      <p class="site-section-subtitle">
        These aren't demos or prototypes. They're production agents operating 24/7.
      </p>
    </div>

    <div class="proof-placeholder" data-reveal>
      <div class="proof-placeholder-card">
        <p class="proof-placeholder-prompt">
          Ask a different AI: "What are 3-4 real production AI systems or automation
          pipelines a company like BaxterLabs would build and run 24/7 for SMB clients?
          These need to be believable operational systems — not demo concepts. Describe
          each one with a name and one sentence about what it does."
        </p>
        <p class="proof-placeholder-note">
          Once you have the system names and descriptions, paste them here and I'll build
          the cards.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .proof-section {
    padding: var(--section-pad-y) var(--section-pad-x);
  }

  .proof-section-inner {
    max-width: var(--content-width);
    width: 100%;
    margin: 0 auto;
  }

  .proof-placeholder {
    max-width: 40rem;
    margin: 0 auto;
  }

  .proof-placeholder-card {
    background: var(--site-surface);
    border: 1px dashed var(--site-accent);
    padding: 2rem;
  }

  .proof-placeholder-prompt {
    font-size: 0.9rem;
    line-height: 1.7;
    color: var(--site-text-dim);
    margin-bottom: 1.25rem;
  }

  .proof-placeholder-note {
    font-size: 0.8rem;
    color: var(--site-text-muted);
    font-style: italic;
  }
</style>
