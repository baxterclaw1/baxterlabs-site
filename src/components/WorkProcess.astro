<section class="work-section" id="what-to-expect">
  <div class="work-section-inner">
    <div class="site-section-head" data-reveal>
      <h2 class="site-section-title">What to expect</h2>
      <p class="site-section-subtitle">
        Save time, save money, expand your bandwidth.
      </p>
    </div>

    <div class="timeline" data-reveal>
      <div class="timeline-track" aria-hidden="true"></div>

      <div class="timeline-step">
        <div class="timeline-dot" aria-hidden="true">1</div>
        <h3 class="timeline-title">Discover</h3>
        <p class="timeline-desc">
          A conversation about your operation. Together we find what
          slows you down and if our services are a good fit.
        </p>
      </div>

      <div class="timeline-step">
        <div class="timeline-dot" aria-hidden="true">2</div>
        <h3 class="timeline-title">Propose</h3>
        <p class="timeline-desc">
          You get a written proposal. Scope, pricing, timeline, and how
          we integrate with your existing systems.
        </p>
      </div>

      <div class="timeline-step">
        <div class="timeline-dot" aria-hidden="true">3</div>
        <h3 class="timeline-title">Build</h3>
        <p class="timeline-desc">
          A weekly meeting keeps progress transparent, while also
          incorporating your input to keep the vision aligned.
        </p>
      </div>

      <div class="timeline-step">
        <div class="timeline-dot" aria-hidden="true">4</div>
        <h3 class="timeline-title">Run</h3>
        <p class="timeline-desc">
          We monitor, we maintain, we adapt as your business and
          technology evolve. Long-term relationships over volume.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .work-section {
    padding: var(--section-pad-y) var(--section-pad-x);
  }

  .work-section-inner {
    max-width: var(--content-width);
    width: 100%;
    margin: 0 auto;
  }

  .timeline {
    position: relative;
    max-width: 62rem;
    margin: 0 auto;
    display: flex;
    gap: 0;
    padding: 0 0 1.5rem;
  }

  .timeline-track {
    position: absolute;
    /* Run from center of step 1 to center of step 4 so both ends meet the dots */
    left: 12.5%;
    right: 12.5%;
    top: 1.5rem;
    height: 1px;
    background: var(--site-border);
  }

  .timeline-step {
    position: relative;
    padding-top: 3.5rem;
    flex: 1;
    min-width: 0;
    text-align: center;
  }

  .timeline-dot {
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 3rem;
    height: 3rem;
    border-radius: 50%;
    border: 1.5px solid var(--site-accent);
    background: var(--site-bg);
    color: var(--site-accent);
    font-size: 1rem;
    font-weight: 600;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 1;
  }

  .timeline-title {
    font-size: clamp(1.1rem, 1.7vw, 1.25rem);
    font-weight: 600;
    letter-spacing: -0.01em;
    color: var(--site-text);
    margin-bottom: 0.6rem;
  }

  .timeline-desc {
    font-size: clamp(0.92rem, 1.25vw, 1.02rem);
    line-height: 1.6;
    color: var(--site-text-dim);
    padding: 0 0.5rem;
    max-width: 15.5rem;
    margin: 0 auto;
  }

  @media (max-width: 640px) {
    .timeline {
      flex-direction: column;
      gap: 0;
    }

    .timeline-track {
      left: 1rem;
      right: auto;
      top: 0;
      bottom: 0;
      width: 1px;
      height: auto;
    }

    .timeline-step {
      display: grid;
      grid-template-columns: auto 1fr;
      gap: 0 1.25rem;
      padding: 0 0 2.5rem;
      text-align: left;
    }

    .timeline-step:last-child {
      padding-bottom: 0;
    }

    .timeline-dot {
      position: static;
      transform: none;
      grid-column: 1;
    }

    .timeline-title,
    .timeline-desc {
      grid-column: 2;
    }

    .timeline-desc {
      padding: 0;
    }
  }
</style>
