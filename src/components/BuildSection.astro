<section class="build-section" id="what-we-build">
  <div class="build-section-inner">
    <div class="site-section-head" data-reveal>
      <span class="site-section-eyebrow">What We Build</span>
      <h2 class="site-section-title">Everything AI can automate, we can build.</h2>
      <p class="site-section-subtitle">
        Each of these is a system we design and deploy for real businesses.
        No templates, no cookie-cutter setups.
      </p>
    </div>

    <ul class="build-list" data-reveal>
      <li class="build-item">
        <h3 class="build-item-title">Work that handles itself</h3>
        <p class="build-item-desc">Autonomous agents that monitor your operations, make decisions, and take action — 24/7, without a human in the loop.</p>
      </li>
      <li class="build-item">
        <h3 class="build-item-title">Tools that talk to each other</h3>
        <p class="build-item-desc">Connect your existing software so data moves automatically. No more copying from QuickBooks into a spreadsheet into an email.</p>
      </li>
      <li class="build-item">
        <h3 class="build-item-title">Client records that stay current</h3>
        <p class="build-item-desc">Custom CRM and database systems that track your customers, cases, and deals — synced across every tool you use.</p>
      </li>
      <li class="build-item">
        <h3 class="build-item-title">Dashboards that tell you what matters</h3>
        <p class="build-item-desc">Real-time operational views of your business. Not vanity metrics. The numbers that actually drive decisions.</p>
      </li>
      <li class="build-item">
        <h3 class="build-item-title">Reports that compile themselves</h3>
        <p class="build-item-desc">Scheduled data collection, processing, and delivery. Weekly reports, inventory snapshots, pipeline summaries — ready when you need them.</p>
      </li>
      <li class="build-item">
        <h3 class="build-item-title">Documents that process themselves</h3>
        <p class="build-item-desc">Incoming forms, contracts, and emails classified, extracted, and routed automatically. No manual data entry.</p>
      </li>
      <li class="build-item">
        <h3 class="build-item-title">Follow-ups that don't get forgotten</h3>
        <p class="build-item-desc">Automated client communication sequences. Reminders, check-ins, and status updates that fire on schedule.</p>
      </li>
      <li class="build-item">
        <h3 class="build-item-title">AI that fits your existing stack</h3>
        <p class="build-item-desc">Smart search, auto-categorization, and chat interfaces layered on top of the tools you already use — not replacing them.</p>
      </li>
    </ul>
  </div>
</section>

<style>
  .build-section {
    padding: var(--section-pad-y) var(--section-pad-x);
  }

  .build-section-inner {
    max-width: var(--content-width);
    width: 100%;
    margin: 0 auto;
  }

  .build-list {
    list-style: none;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem 3rem;
    max-width: 56rem;
    margin: 0 auto;
  }

  .build-item-title {
    font-size: 0.95rem;
    font-weight: 600;
    letter-spacing: -0.01em;
    color: var(--site-text);
    margin-bottom: 0.35rem;
  }

  .build-item-desc {
    font-size: 0.85rem;
    line-height: 1.6;
    color: var(--site-text-dim);
    max-width: 24rem;
  }

  @media (max-width: 600px) {
    .build-list {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }
  }
</style>
