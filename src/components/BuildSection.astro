<section class="build-section" id="built-for-how-you-run">
  <div class="build-section-inner">
    <div class="site-section-head" data-reveal>
      <h2 class="site-section-title">Built for how you run</h2>
      <p class="site-section-subtitle">
        Some examples of real systems we've designed and deployed.
      </p>
    </div>

    <div class="build-stagger">
      <div class="build-row">
        <div class="build-media">
          <img class="is-dim" src="/images/n8n-workflow.png?v=1" width="800" height="500" loading="eager" decoding="async" alt="" />
        </div>
        <div class="build-copy">
          <h3 class="build-title">Workflow automation</h3>
          <p class="build-desc">The repetitive steps between your tools, automated. Client intake, invoicing, scheduling, approvals. Data flows where it needs to go without someone retyping it or remembering to push it along.</p>
        </div>
      </div>

      <div class="build-row">
        <div class="build-copy">
          <h3 class="build-title">Dashboards and reports</h3>
          <p class="build-desc">One clear view of how your operation is actually running. Revenue, pipeline, utilization, whatever metrics matter to you. A single source of truth that stays current on its own.</p>
        </div>
        <div class="build-media">
          <img src="/images/dashboard.png?v=1" width="800" height="500" loading="eager" decoding="async" alt="" />
        </div>
      </div>

      <div class="build-row">
        <div class="build-media">
          <img class="is-soft" src="/images/doc-processing.png?v=4" width="800" height="500" loading="eager" decoding="async" alt="" />
        </div>
        <div class="build-copy">
          <h3 class="build-title">Document processing</h3>
          <p class="build-desc">Forms, contracts, invoices. Extracted, classified, and routed as they arrive. Filed, tagged, and sent to the right person. Automatically.</p>
        </div>
      </div>

      <div class="build-row">
        <div class="build-copy">
          <h3 class="build-title">CRM and client systems</h3>
          <p class="build-desc">Custom databases built around how you track relationships. Contacts, deals, communication history, follow-ups. Synced across the tools you already use.</p>
        </div>
        <div class="build-media">
          <img src="/images/crm-preview.png?v=2" width="800" height="500" loading="eager" decoding="async" alt="" />
        </div>
      </div>

      <div class="build-row">
        <div class="build-media">
          <img class="is-art" src="/images/agents.svg?v=6" width="800" height="500" loading="eager" decoding="async" alt="" />
        </div>
        <div class="build-copy">
          <h3 class="build-title">AI integration</h3>
          <p class="build-desc">AI agents that watch, decide, and act on their own or through a human-in-the-loop. Monitoring, triaging, flagging what needs attention. Running 24/7 on your infrastructure, governed by your rules.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .build-section {
    padding: var(--section-pad-y) var(--section-pad-x);
  }

  .build-section-inner {
    max-width: var(--content-width);
    width: 100%;
    margin: 0 auto;
  }

  .build-stagger {
    display: flex;
    flex-direction: column;
    gap: 3rem;
  }

  .build-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2.5rem;
    align-items: center;
  }

  .build-media {
    line-height: 0;
    border: 1px solid var(--site-border);
    overflow: hidden;
  }

  .build-media img {
    display: block;
    width: 100%;
    height: auto;
    aspect-ratio: 16/10;
    object-fit: cover;
    filter: brightness(0.92) contrast(1.05) saturate(0.75);
  }

  .build-media img.is-dim {
    filter: brightness(0.65) contrast(1.05) saturate(0.4);
  }

  .build-media img.is-soft {
    filter: brightness(0.8) contrast(1.05) saturate(0.65);
  }

  .build-media img.is-art {
    filter: none;
    object-fit: contain;
    background: #0a0b0d;
  }

  .build-copy {
    display: flex;
    flex-direction: column;
    justify-content: center;
    text-align: center;
  }

  .build-title {
    font-size: clamp(1.2rem, 2.2vw, 1.6rem);
    font-weight: 600;
    letter-spacing: -0.02em;
    color: var(--site-accent);
    margin-bottom: 0.75rem;
  }

  .build-desc {
    font-size: clamp(0.95rem, 1.3vw, 1.05rem);
    line-height: 1.7;
    color: var(--site-text-dim);
    margin-left: auto;
    margin-right: auto;
    max-width: 26rem;
  }

  @media (max-width: 640px) {
    .build-row {
      grid-template-columns: 1fr;
      gap: 1rem;
    }

    .build-media {
      order: -1;
    }
  }
</style>
