<section class="who" id="who">
  <div class="who-inner">
    <div class="site-section-head">
      <h2 class="site-section-title">Who this is for</h2>
    </div>
    <div class="who-copy" data-reveal>
      <p>
        Illinois property tax appeal firms that file in Cook County. Usually
        shops where season volume has outgrown retyping, spreadsheets, and
        chasing signatures in email.
      </p>
      <p class="who-not">
        If you do general litigation, or you want a tool so property owners can
        appeal on their own, this page isn't for you.
      </p>
    </div>
  </div>
</section>

<style>
  .who {
    padding: var(--section-pad-y) var(--section-pad-x);
  }

  .who-inner {
    max-width: var(--content-width);
    width: 100%;
    margin: 0 auto;
  }

  .who-copy {
    max-width: 40rem;
    margin: 0 auto;
    text-align: center;
  }

  .who-copy p {
    font-size: clamp(1.15rem, 1.8vw, 1.35rem);
    line-height: 1.65;
    color: var(--site-text);
    text-wrap: pretty;
  }

  .who-not {
    margin-top: 1.25rem;
    color: var(--site-text-dim) !important;
    font-size: clamp(1rem, 1.4vw, 1.125rem) !important;
  }
</style>
